/**
 * Error handler
 */
class ErrorHandler {
    /**
     * error handler empty storage
     */
    static onErrorEmptyStorage() {
        Logger.log('empty storage exit');
    }

    /**
     * error handler no today events
     */
    static onErrorNoTodayEvents() {
        Logger.log('no today events, exit');
    }
}